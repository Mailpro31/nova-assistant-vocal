# ==========================================================
#  Nova — assistant vocal Windows — dépendances Python
#  Python 3.11 / 3.12 / 3.13. Installer :  pip install -r requirements.txt
# ==========================================================

# --- Reconnaissance vocale (Whisper local) ---
faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.24.0

# --- Interface (fenêtre bleu nuit + pilule tkinter) ---
pywebview>=4.0.0
pystray>=0.19.0
Pillow>=10.0.0

# --- Contrôle Windows ---
keyboard>=0.13.5
pycaw>=20230407              # volume système / par application
comtypes>=1.4.0
psutil>=5.9.0
pywin32>=306                 # win32 (facultatif selon la plateforme)

# --- Voix de synthèse ---
edge-tts>=6.1.0             # voix neuronale en ligne (fallback : SAPI Windows natif)

# --- Réseau & intégrations ---
requests>=2.28.0

# --- Fuseaux horaires (« quelle heure à Tokyo ») ---
tzdata>=2024.1

# ---------- OPTIONNEL (imports paresseux, Nova dégrade proprement si absent) ----------
# pvporcupine>=3.0.0        # mot d'éveil Porcupine (alternative à Whisper)
# opencv-python>=4.8.0      # webcam (« regarde-moi »)
# youtube-transcript-api>=0.6.0   # résumé de vidéos YouTube
# anthropic>=0.40.0        # SDK Claude (sinon appel HTTP direct)
# pyserial>=3.5            # OBD-II / véhicule (ELM327)

# ---------- Externe (hors pip) ----------
# ffmpeg  dans le PATH  → conversion des flux IPTV pour le navigateur
# Ollama  (ollama.com)  → modèles IA 100 % locaux (facultatif)
